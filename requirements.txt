# JamBuddy - core deps (no training)
# Use Python 3.10, 3.11, or 3.12 (3.14+ often has no binary wheels yet).
numpy>=1.23
scipy>=1.10
sounddevice>=0.4.6
librosa>=0.10
pyfluidsynth>=1.3
mido>=1.3
requests>=2.28
# Optional: for AI chord backing, install realjam and run realjam-start-server in another terminal.
# realjam>=1.0
